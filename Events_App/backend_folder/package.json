{
    "name": "my-nodejs-services",
    "version": "1.0.0",
    "description": "",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build:frontend": "cd .. && npm --prefix frontend install --no-audit --progress=false && npm --prefix frontend run build && cd backend && rm -rf dist && mkdir -p dist && cp -R ../frontend/dist/. dist/",
        "gcp-build": "echo '⚠️  gcp-build skipped - frontend must be built locally before deployment'",
        "predeploy": "npm run build:frontend"
    },
    "dependencies": {
    "cors": "^2.8.5",
    "detect-port": "^1.6.1",
        "dotenv": "^17.2.3",
        "express": "^4.21.2",
        "mongodb": "^6.20.0",
        "mongosh": "^2.5.9",
        "node-fetch": "^2.7.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.10"
    }
}
