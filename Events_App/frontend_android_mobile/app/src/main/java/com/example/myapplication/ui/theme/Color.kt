package com.example.myapplication.ui.theme

import androidx.compose.ui.graphics.Color

// ---- DARK THEME ----
val DarkPrimaryContainer = Color(0xFF1F2A44)      // Dark blue header
val DarkOnPrimaryContainer = Color(0xFFFFFFFF)    // White text/icons

val DarkBackground = Color(0xFF000000)            // Pure black background
val DarkSurface = Color(0xFF1A1A1A)               // Slightly lighter dark for cards
val DarkOnBackground = Color(0xFFFFFFFF)          // White text
val DarkOnSurface = Color(0xFFFFFFFF)             // White text

// Surface variant for footer
val DarkSurfaceVariant = Color(0xFF1E1E1E)        // Slightly lighter than surface

// ---- LIGHT THEME ----
val LightPrimaryContainer = Color(0xFFA0BDEA)      // Light blue-purple header (like image)
val LightOnPrimaryContainer = Color(0xFF1A237E)   // Dark blue text/icons on header

val LightBackground = Color(0xFFF5F5F5)           // Light grey background
val LightSurface = Color(0xFFFFFFFF)              // White cards
val LightSurfaceVariant = Color(0xFFF0F0F0)        // Light grey for cards (use this for cards)
val LightOnBackground = Color(0xFF1C1C1F)         // Dark text
val LightOnSurface = Color(0xFF1C1C1F)            // Dark text           // Dark text